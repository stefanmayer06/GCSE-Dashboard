import { ApiError, deleteAccount, errorFromResponse, normalizeBaseUrl } from '../api';
test('normalizes API origins',()=>expect(normalizeBaseUrl(' https://example.com/// ')).toBe('https://example.com'));
test('rejects relative API origins',()=>expect(()=>normalizeBaseUrl('/api')).toThrow('absolute'));
test('creates structured JSON errors',async()=>{const error=await errorFromResponse({status:409,json:async()=>({error:'Taken',code:'conflict'})} as Response);expect(error).toEqual(expect.objectContaining({message:'Taken',status:409,code:'conflict'}));expect(error).toBeInstanceOf(ApiError)});
test('deletes the authenticated account without sending a user ID',async()=>{process.env.EXPO_PUBLIC_API_URL='https://api.example.com/';const request=jest.fn(async()=>({ok:true,json:async()=>({success:true})})) as unknown as typeof fetch;await deleteAccount('access-token','DELETE',request);expect(request).toHaveBeenCalledWith('https://api.example.com/api/auth/account',expect.objectContaining({method:'DELETE',headers:{Accept:'application/json','Content-Type':'application/json',Authorization:'Bearer access-token'},body:JSON.stringify({confirmation:'DELETE'}),signal:expect.any(AbortSignal)}))});
