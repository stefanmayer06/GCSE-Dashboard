{
  "name": "maths-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "AQA GCSE Foundation Maths training dashboard",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "node --test server/test/*.test.js",
    "test:ui": "npx playwright test ui-tests/ui.spec.js --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "concurrently": "^9.1.0"
  }
}
