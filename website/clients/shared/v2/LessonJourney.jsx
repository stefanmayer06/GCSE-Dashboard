import { useState } from 'react';
import { Link } from 'react-router-dom';
export default function LessonJourney({topic}){const [recall,setRecall]=useState(false);return <section className="lesson-journey" aria-label="Your learning session"><nav aria-label="Lesson steps"><a href="#understand">Understand</a><span aria-hidden="true">→</span><a href="#retrieve">Practice & recall</a><span aria-hidden="true">→</span><Link to="/notebook">Revisit</Link></nav><div className="lesson-recall"><button className="btn" aria-expanded={recall} onClick={()=>setRecall(!recall)}>{recall?'Close recall prompt':'Before you read: what do you remember?'}</button>{recall&&<p>Without looking at the notes, explain <strong>{topic}</strong> in your own words. Name one step you know and one question you have. Then read to fill the gap.</p>}</div><Link to={`/chat?prompt=${encodeURIComponent(`Help me understand ${topic}. Ask me what I already know, then explain the method step by step.`)}`}>Talk this topic through with your tutor →</Link></section>}
